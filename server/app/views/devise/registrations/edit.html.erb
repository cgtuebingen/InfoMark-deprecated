<h2>Mein Account bearbeiten</h2>
<p>Deine Daten speichern wir geschützt auf dem Fachbereich-Server innerhalb der Universitäts. Es werden <b>keine</b> Daten extern gehalten und gespeichert.</p>
<%= bootstrap_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>
  <%= f.text_field :first_name, autofocus: true, label: "Vorname" %>
  <%= f.text_field :last_name, label: "Familienname"  %>
  <div class="row">
    <div class="col-md-6">
      <%= f.password_field :password, autocomplete: "off", label: "neues Passwort ",help: "Nichts eingeben, wenn Passwort beibehalten werden soll." %>
      </div>
    <div class="col-md-6">
      <%= f.password_field :password_confirmation, autocomplete: "off", label: "neues Passwort bestätigen" ,help: "Nichts eingeben, wenn Passwort beibehalten werden soll." %>
      </div>
  </div>
  <h3>weitere Daten</h3>
  <p>Diese Daten sind für keine registrierten Personen sichtbar. Diese werden nur für interne Statistiken und für die Klausurzulassen verwendet. </p>
  <%= f.text_field :semester, label: "Fachsemester"  %>
  <%= f.text_field :student_number, label: "Matrikelnummer"  %>
  <%= f.text_field :subject, label: "Studienfach"  %>
  
  <h3>optional</h3>
  <div class="row">
    <div class="col-md-6"><%= gravatar_for @user %> <%= f.file_field :avatar %></div>
  </div>
  <h3>Aktionen</h3>
  <%= f.password_field :current_password, autocomplete: "off",label: "aktuelles Passwort", help: "we need your current password to confirm your changes" %>
  <div class="row"> 
    <div class="col-md-6 pull-right">
      <%= f.primary "Änderung durchführen" %>
    </div>
  </div>
<% end %>
<!---
<h3>Cancel my account</h3>
<p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>
<%= link_to "Back", :back %>
-->